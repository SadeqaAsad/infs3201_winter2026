<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Employee Scheduling System</title>
</head>
<body>
    <h1>List of Employees</h1>
    <ul>
        {{#each employees}}
        <li><a href="/employee/{{_id}}">{{name}}</a></li>
        {{/each}}
    </ul>
</body>
</html>
